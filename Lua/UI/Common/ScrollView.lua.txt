
--- 没用

CSScrollViewItem = {}

function CSScrollViewItem:New(index,prefab,clickEvent)
    c = {
        Index = index,  -- 编号
        Prefab = prefab,  -- 控件
        ClickEvent = clickEvent, -- 点击事件
    }
    setmetatable(c, self)
    self.__index = self
    return c
end


function CSScrollViewItem:ClickEvent()
    print(self.Index)
end 